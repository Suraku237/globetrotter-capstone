# Only web/ and android/ are needed inside the image (see Dockerfile) —
# everything else here just bloats the build context.
build/
.dart_tool/
.idea/
.vscode/
android/.gradle/
android/app/build/
ios/
macos/
linux/
windows/
*.iml
*.log
.packages
.pub-cache/
